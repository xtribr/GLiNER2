# Production requirements - CPU-only for Fly.io
fastapi==0.115.6
uvicorn[standard]==0.34.0
pandas>=2.2,<3.1
# Stack ML PINADO p/ casar com os artefatos treinados (joblibs salvos em sklearn 1.8.0).
# train==serve: afrouxar estas versões impede o carregamento dos modelos em prod → predição vira 422.
numpy==2.4.6
scikit-learn==1.8.0
pydantic[email]
python-multipart
xgboost>=2.0,<4.0
joblib>=1.3,<2.0
scipy==1.17.1
requests==2.32.3

# Authentication (legacy - being replaced by Supabase)
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1

# Supabase (new)
supabase==2.10.0

# Database (legacy - being replaced by Supabase)
sqlalchemy
psycopg2-binary
python-dotenv

# Email
resend
