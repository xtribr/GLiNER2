[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[build.args]
NEXT_PUBLIC_API_URL = "https://alert-trust-production.up.railway.app"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
